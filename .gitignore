# OS
.DS_Store
Thumbs.db

# 환경 변수 (비밀값은 .env만 — 커밋 금지, PLAN.md §12)
.env
.env.local
.env.*.local

# Node
node_modules/
dist/
build/
.vite/
*.log

# Python (backend 트랙용)
__pycache__/
.venv/
*.pyc

# 에디터
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# Claude Code 로컬 툴링 (개인 설정/스킬 — 전체 커밋 금지)
.claude/

# 외부 저장소 클론 (자체 .git 포함 — 커밋 금지)
claude-code-action/

# 로컬 캐시
.cache/

# 수동 업데이트
PLAN.md
Setup_Guide_Windows.md
tmp/
